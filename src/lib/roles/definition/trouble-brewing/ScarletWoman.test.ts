import { it, expect } from "vitest";

// Scarlet Woman is a passive role — all behavior is on the scarlet_woman effect.
// See src/lib/effects/definition/ScarletWoman.test.ts for handler tests.
it("Scarlet Woman has no testable features (behavior is on effect)", () => {
    expect(true);
});
