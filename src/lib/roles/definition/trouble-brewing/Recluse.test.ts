import { it, expect } from "vitest";

// Recluse's only feature is the "recluse_misregister" perception modifier effect.
// The perception behavior is tested in RecluseMisregister.test.ts.
it("Recluse's perception behavior is tested in RecluseMisregister.test.ts", () => {
    expect(true);
});
